// TODO get url from config
const baseURL = 'http://localhost:4000/api'

function setupUser({ userid, timezone, weekstart, currency, req}) {
  // find user
  // aggregate all details (userid, first/last name, email, timezone, weekstart, currency)
  // make post request to baseUrl/users/setup
  // TODO will need to get the auth token from greenlit
}
